{
  "name": "PixelCons-Core",
  "version": "1.0.0",
  "main": "index.js",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "clean": "rimraf ./cache && rimraf ./artifacts && rimraf ./build",
    "compile": "rimraf ./cache && rimraf ./artifacts && hardhat compile",
    "deploy": "hardhat run --network local scripts/deploy.js",
    "test": "rimraf ./cache && rimraf ./artifacts && hardhat test",
    "web_build": "node scripts/webbuild.js",
    "web_start": "node scripts/webapp.js",
    "web_debug": "node scripts/webapp.js -debug"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.7",
    "hardhat": "^2.6.4",
    "mocha": "^9.1.2",
    "rimraf": "^3.0.2",
    "ts-node": "^10.2.1",
    "node-watch": "^0.7.2",
    "typescript": "^4.4.3",
    "html-minifier": "^4.0.0",
    "uglify-js": "^3.14.2",
    "uglifycss": "^0.0.29"
  },
  "dependencies": {
    "express": "^4.17.1",
    "fast-png": "^5.0.4",
    "qrcode": "^1.4.4"
  }
}
