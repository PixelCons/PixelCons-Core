{
  "name": "PixelCons-Core",
  "version": "1.0.0",
  "main": "index.js",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "clean": "rimraf ./cache && rimraf ./artifacts && rimraf ./build",
    "compile": "rimraf ./cache && rimraf ./artifacts && hardhat compile",
	"deploy": "hardhat run --network optimism_l1 scripts/deploy.js",
    "test": "rimraf ./cache && rimraf ./artifacts && hardhat test",
    "web_start": "node scripts/webapp.js",
    "web_build": "node scripts/webapp.js -build",
    "web_debug": "node scripts/webapp.js -debug"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.1",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.2.0",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.2.1",
    "ethers": "^5.0.24",
    "hardhat": "^2.0.7",
    "html-minifier": "^4.0.0",
    "mocha": "^8.2.1",
    "node-watch": "^0.7.1",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3",
    "uglify-js": "^3.13.1",
    "uglifycss": "^0.0.29"
  }
}
